# ChatTTS 毕设 API 环境变量配置

# Mock 模式：设为 true 可在无 GPU 环境调试接口
MOCK_MODE=false

# 模型源：huggingface, local, custom
MODEL_SOURCE=custom

# 自定义模型路径 (当 MODEL_SOURCE=custom 时使用)
# MODEL_PATH=/path/to/ChatTTS-weights

# 音色存储路径
SPEAKER_STORAGE_PATH=./speaker_storage

# API 配置
API_HOST=0.0.0.0
API_PORT=8000
